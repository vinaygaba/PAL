main()
{
	l1 : list list list int;
	a : list list int;
	b : list int;
	c : int;
	d : list string;
	l1 += a;
	a += b;
	b +=c ;
	b -= [1];
	m1 : map int, list string;
	m1 += c,d;
	m1 -= c;


}

writeParagraph(x : int; ) : string {
	
}